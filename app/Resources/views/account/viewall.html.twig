{% extends 'base.html.twig' %}

{%  block title %}
    Users
{% endblock %}

{% block body %}
    <div class="container content">
        <div class="title">Users</div>
        <table class="events-table">
            <thead>
            <tr>
                <td>#</td>
                <td class="events-table-middle">Username</td>
                <td></td>
            </tr>
            </thead>
            <tbody>
            {% for user in users %}
                <tr>
                    <td class="events-table-index">{{ user.id }}</td>
                    <td class="events-table-middle">{{ user.username }}</a></td>
                    <td><a href="{{ path('accountview',{'id':user.getId()}) }}">Profile</a></td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

{% endblock %}